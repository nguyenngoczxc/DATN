@model TTTN3.Models.brand

@{
    ViewData["Title"] = "Delete Confirmation";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<h2>Xác nhận xóa</h2>

<p>Bạn có chắc chắn muốn xóa?</p>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Mã
        </dt>
        <dd class="col-sm-10">
            @ViewBag.BrandCode
        </dd>
        <dt class="col-sm-2">
            Nhà sản xuất
        </dt>
        <dd class="col-sm-10">
            @ViewBag.BrandName
        </dd>
    </dl>
    <div>
        <form method="post" asp-action="Delete_BrandConfirmed" asp-route-brand_Code="@ViewBag.BrandCode" id="deleteForm">
            @Html.AntiForgeryToken()
            <button id="deleteButton" type="button" class="btn btn-danger">Xóa</button>
            <a asp-action="List_Brand" class="btn btn-secondary">Hủy</a>
        </form>
    </div>
</div>




<script>
    // JavaScript to handle the confirmation
    document.getElementById("deleteButton").addEventListener("click", function () {
        var confirmed = confirm("Are you sure you want to delete this brand?");
        if (confirmed) {
            document.getElementById("deleteForm").submit();
        }
    });
</script>
