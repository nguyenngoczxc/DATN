@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Thêm quyền";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

@model TTTN3.Models.ViewModels.AddRoleViewModel


<h2>Thêm quyền cho người dùng</h2>

<div>
    <form asp-action="AddRole" asp-controller="Account" method="post">
        <div class="form-group">
            <label asp-for="UserId" class="control-label">Mã người dùng</label>
            <input asp-for="UserId" class="form-control" readonly />
        </div>
         <div class="form-group">
            <label asp-for="UserName" class="control-label">Tên người dùng</label>
            <input asp-for="UserName" class="form-control" readonly />
        </div> 
        <div class="form-group">
            <label for="roleName">Quyền</label>
            <select class="form-control" id="roleName" name="roleName">
                @foreach (var roleName in Model.RoleNames)
                {
                    <option value="@roleName">@roleName</option>
                }
            </select>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">Thêm</button>
            <a asp-action="Index_account" class="btn btn-secondary">Danh sách</a>
        </div>
    </form>
</div>


